<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcoFit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
<!--        <item name="colorPrimary">@color/cardview_dark_background</item>-->
        <item name="android:popupTheme">@style/AppTheme.PopupOverlay</item>

    </style>

    <style name="Theme.EcoFit" parent="Base.Theme.EcoFit" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

        <!-- Другие цвета для ночного режима -->

</resources>