<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LexiSphere" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LexiSphere" parent="Base.Theme.LexiSphere" />

    <style name="AppSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:queryHint">Search</item>
        <item name="android:iconifiedByDefault">false</item>

        <item name="queryHint">Search</item>

    </style>
    <style name="AppSearchViewStyle1" parent="TextAppearance.Material3.SearchView">
        <item name="android:queryBackground">@null</item>
        <!-- Другие настройки стиля -->
    </style>


    <style name="HomeButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/background</item>
        <item name="android:textColor">@android:color/white</item> <!-- Цвет текста кнопки -->
        <item name="android:textAllCaps">false</item> <!-- Отключение автоматического перевода текста в верхний регистр -->
        <item name="android:backgroundTint">#0003D6</item> <!-- Задаем цвет фона кнопки -->
        <!-- Другие атрибуты, если необходимо -->
    </style>

    <style name="ApSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:backgroundTint">?attr/colorPrimary</item>
        <item name="android:queryBackground">@null</item>
        <!-- Другие настройки стиля -->
    </style>



</resources>